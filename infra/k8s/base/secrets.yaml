# DO NOT commit real values — use `kubectl create secret` or external-secrets operator
apiVersion: v1
kind: Secret
metadata:
  name: eventflow-oauth-secrets
  namespace: eventflow
type: Opaque
stringData:
  google-client-id: REPLACE_ME
  google-client-secret: REPLACE_ME
  internal-token: REPLACE_ME
