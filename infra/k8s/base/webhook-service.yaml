apiVersion: apps/v1
kind: Deployment
metadata:
  name: webhook-service
  labels:
    app: webhook-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: webhook-service
  template:
    metadata:
      labels:
        app: webhook-service
    spec:
      containers:
        - name: webhook-service
          image: ghcr.io/aloysJehwin/kafka-k8s-playground/webhook-service:latest
          ports:
            - containerPort: 8091
          env:
            - name: DB_URL
              value: "jdbc:postgresql://postgres-webhooks:5432/webhooks"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: eventflow-secrets
                  key: webhook-db-user
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: eventflow-secrets
                  key: webhook-db-password
            - name: KAFKA_BOOTSTRAP
              value: "kafka:9092"
            - name: SCHEMA_REGISTRY_URL
              value: "http://schema-registry:8081"
            - name: SERVER_PORT
              value: "8091"
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 300m
              memory: 384Mi
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8091
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8091
            initialDelaySeconds: 20
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: webhook-service
spec:
  selector:
    app: webhook-service
  ports:
    - port: 8091
      targetPort: 8091
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: webhook-service-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: webhook-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
